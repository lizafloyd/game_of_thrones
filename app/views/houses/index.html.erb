<h1>Noble Houses</h1>

<% @houses.each do |house| %>
  <div>
    <h2><%= link_to "#{house.name}", "houses/#{house.id}" %></h2>
    <h4><%= house.words %></h4>
    <%= image_tag "#{house.sigil_url}" %>
  </div>
<% end %>
